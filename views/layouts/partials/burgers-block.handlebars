<li>
	{{name}}

	<button class="change-status" data-id="{{id}}" data-devoured="{{this.devoured}}">
		{{#if devoured}}EATEN!{{else}}EAT IT!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Delete Me!</button>
</li>
